import {StyleSheet} from 'react-native';
import color from '../../styles/color';

export default StyleSheet.create({
    container : {
        margin: 10,
        alignSelf:'center',
    },
    text_style: {
        color: color.grey700,
        fontFamily: 'Jua',
        fontSize: 15,
        textAlign: 'center'
    }
})
